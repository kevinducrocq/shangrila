{% extends "base.html.twig" %}

{% block title %}Admin - Edition d'un menu
{% endblock %}

{% block body %}
	{% include "/admin/_sidebar_admin_mobile.html.twig" %}

	<div class="container mt-5 mb-5">

		<div class="d-flex justify-content-between align-items-center">
			<h1 class="h2 mb-5">Edition d'un menu</h1>
		</div>

		<div class="row">

			<div class="col-md-3" id="navi">
				{% include "/admin/_sidebar-admin.html.twig" %}
			</div>

			<div class="bg-light shadow p-5 col-md-9">
				{{form_start(foodEditForm)}}
				{{form_widget(foodEditForm)}}
				<button type="submit" class="btn btn-primary">Modifier</button>
				<a href="{{path('show_food')}}" class="btn btn-dark">
					<i class="fas fa-backward"></i>
					Retour</a>
				{{form_end(foodEditForm)}}
			</div>
		</div>
	</div>


{% endblock %}
