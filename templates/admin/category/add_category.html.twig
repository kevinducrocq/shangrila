{% extends "base.html.twig" %}

{% block title %}Admin - Ajouter une catégorie
{% endblock %}

{% block body %}

	<div class="container mt-5 mb-5">
		{% include "/admin/_sidebar_admin_mobile.html.twig" %}
		<div class="d-flex justify-content-between align-items-center">
			<h1 class="h2 mb-5">Ajouter une catégorie</h1>
		</div>

		<div class="row">

			<div class="col-md-3">
				{% include "/admin/_sidebar-admin.html.twig" %}
			</div>

			<div class="col-md-9">
				{{form_start(categoryForm)}}
				{{form_widget(categoryForm)}}
				<button type="submit" class="btn btn-primary">Ajouter</button>
				<a href="{{path('show_category')}}" class="btn btn-dark">
					<i class="fas fa-backward"></i>
					Retour</a>
				{{form_end(categoryForm)}}
			</div>
		</div>
	</div>

{% endblock %}
