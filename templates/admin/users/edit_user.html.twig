{% extends "base.html.twig" %}

{% block body %}

	<div class="container mt-5">
	{% include "/admin/_sidebar_admin_mobile.html.twig" %}

		<div class="d-flex justify-content-between align-items-center">
			<h1 class="h2 mb-5">Edition d'un utilisateur</h1>
		</div>

		<div class="row">

			<div class="col-md-3">
				{% include "/admin/_sidebar-admin.html.twig" %}
			</div>

			<div class="bg-light shadow p-5 col-md-9">
				{{form_start(editUserForm)}}
				{{form_widget(editUserForm)}}
				<button type="submit" class="btn btn-primary">Modifier</button>
				<a href="{{path('show_category')}}" class="btn btn-dark"><i class="fas fa-backward"></i> Retour</a>
				{{form_end(editUserForm)}}
			</div>
		</div>
	</div>


{% endblock %}
