{% extends "base.html.twig" %}

{% block title %}Ajouter un commentaire
{% endblock %}

{% block body %}

	<div class="container mt-5 mb-5">


		<div class="col-md-6 p-5 shadow rounded offset-md-3">
			<h1 class="h2 mb-5">Ajouter un commentaire</h1>
			{{form_start(commentForm)}}
			{{form_widget(commentForm)}}
			<button type="submit" class="btn btn-primary">Ajouter</button>
			<a href="{{path('show_category')}}" class="btn btn-dark">
					<i class="fas fa-backward"></i> Retour</a>
			{{form_end(commentForm)}}
		</div>
	</div>
</div></div>{% endblock %}
